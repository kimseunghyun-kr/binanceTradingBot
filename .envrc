# Automatically set up development environment using Nix flake
# This file is used by direnv to automatically activate the environment
# when you cd into this directory

# Use nix flake for development environment
use flake

# Load .env file if it exists
dotenv_if_exists .env

# Add project root to PYTHONPATH
export PYTHONPATH="$PWD:$PYTHONPATH"

# Python settings
export PYTHONUNBUFFERED=1
export PYTHONDONTWRITEBYTECODE=1
export PYTHONFAULTHANDLER=1

# Activate virtual environment if it exists
if [ -d .venv ]; then
  source .venv/bin/activate
fi

# Display welcome message
echo "✅ Development environment loaded via direnv"
